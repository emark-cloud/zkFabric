node_modules/
dist/
cache/
artifacts/
typechain-types/

# Circuit build artifacts (large binaries, regenerated by setup-ceremony.sh)
circuits/build/**/*.wasm
circuits/build/*.zkey
circuits/build/*.wtns
circuits/build/*.r1cs
circuits/build/*.sym
circuits/build/*.ptau

# Keep verification key and hello_world vkey (small JSON)
!circuits/build/verification_key.json
!circuits/build/hello_world_vkey.json

# Frontend build + large circuit artifacts served from public
app/.next/
app/public/circuits/*.wasm
app/public/circuits/*.zkey

# SDK build output
sdk/dist/

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Coverage
coverage/
coverage.json

# Hardhat
hardhat-deployments/
